// Placeholder so the Vitest runner has something to execute before the first
// real component lands. Delete once component specs exist under src/lib.
describe('test infrastructure', () => {
  it('runs', () => {
    expect(true).toBe(true);
  });
});
