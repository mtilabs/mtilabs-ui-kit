---
'@mtilabs/ui': minor
---

Add `MtButton` (`<mt-button>`): a themeable button with `primary`/`secondary`/`destructive`/`ghost` variants, `sm`/`md`/`lg` sizes, and `disabled`/`loading` states. Fully driven by `@mtilabs/ui`'s design tokens, so it adapts to light/dark themes and any token overrides automatically.
